---
name: branch-commit
description: Codex-compatible mirror of `/branch-commit`. Use when the user asks for `/branch-commit`, `$branch-commit`, or wants the repo's branch-and-commit workflow. Read `.ai/commands/branch-commit.md` and follow it as the source of truth.
---

# Branch Commit

When invoked, read [../../commands/branch-commit.md](../../commands/branch-commit.md) and execute that workflow as written.

## Rules

- Treat the command file as canonical.
- In Codex, invoke this workflow with `$branch-commit`.
- If the command file and this skill ever drift, update this skill to match the command.
