---
name: worktree
description: Codex-compatible mirror of `/worktree`. Use when the user asks for `/worktree`, `$worktree`, or wants the repo's worktree creation workflow. Read `.ai/commands/worktree.md` and follow it as the source of truth.
---

# Worktree

When invoked, read [../../commands/worktree.md](../../commands/worktree.md) and execute that workflow as written.

## Rules

- Treat the command file as canonical.
- In Codex, invoke this workflow with `$worktree`.
- If the command file and this skill ever drift, update this skill to match the command.
