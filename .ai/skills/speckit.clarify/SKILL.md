---
name: speckit.clarify
description: Codex-compatible mirror of `/speckit.clarify`. Use when the user asks for `/speckit.clarify`, `$speckit.clarify`, or wants the repo's Speckit clarify workflow. Read `.ai/commands/speckit.clarify.md` and follow it as the source of truth.
---

# Speckit Clarify

When invoked, read [../../commands/speckit.clarify.md](../../commands/speckit.clarify.md) and execute that workflow as written.

## Rules

- Treat the command file as canonical.
- In Codex, invoke this workflow with `$speckit.clarify`.
- If the command file and this skill ever drift, update this skill to match the command.
