---
name: speckit.implement
description: Codex-compatible mirror of `/speckit.implement`. Use when the user asks for `/speckit.implement`, `$speckit.implement`, or wants the repo's Speckit implement workflow. Read `.ai/commands/speckit.implement.md` and follow it as the source of truth.
---

# Speckit Implement

When invoked, read [../../commands/speckit.implement.md](../../commands/speckit.implement.md) and execute that workflow as written.

## Rules

- Treat the command file as canonical.
- In Codex, invoke this workflow with `$speckit.implement`.
- If the command file and this skill ever drift, update this skill to match the command.
