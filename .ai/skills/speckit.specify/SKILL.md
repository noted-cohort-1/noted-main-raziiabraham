---
name: speckit.specify
description: Codex-compatible mirror of `/speckit.specify`. Use when the user asks for `/speckit.specify`, `$speckit.specify`, or wants the repo's Speckit specify workflow. Read `.ai/commands/speckit.specify.md` and follow it as the source of truth.
---

# Speckit Specify

When invoked, read [../../commands/speckit.specify.md](../../commands/speckit.specify.md) and execute that workflow as written.

## Rules

- Treat the command file as canonical.
- In Codex, invoke this workflow with `$speckit.specify`.
- If the command file and this skill ever drift, update this skill to match the command.
