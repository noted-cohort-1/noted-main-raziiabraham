---
name: design-diff
description: Codex-compatible mirror of `/design-diff`. Use when the user asks for `/design-diff`, `$design-diff`, or wants the repo's visual diff workflow against DESIGN.md. Read `.ai/commands/design-diff.md` and follow it as the source of truth.
---

# Design Diff

When invoked, read [../../commands/design-diff.md](../../commands/design-diff.md) and execute that workflow as written.

## Rules

- Treat the command file as canonical.
- In Codex, invoke this workflow with `$design-diff`.
- If the command file and this skill ever drift, update this skill to match the command.
