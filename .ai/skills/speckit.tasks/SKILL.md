---
name: speckit.tasks
description: Codex-compatible mirror of `/speckit.tasks`. Use when the user asks for `/speckit.tasks`, `$speckit.tasks`, or wants the repo's Speckit task-breakdown workflow. Read `.ai/commands/speckit.tasks.md` and follow it as the source of truth.
---

# Speckit Tasks

When invoked, read [../../commands/speckit.tasks.md](../../commands/speckit.tasks.md) and execute that workflow as written.

## Rules

- Treat the command file as canonical.
- In Codex, invoke this workflow with `$speckit.tasks`.
- If the command file and this skill ever drift, update this skill to match the command.
