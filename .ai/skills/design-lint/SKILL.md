---
name: design-lint
description: Codex-compatible mirror of `/design-lint`. Use when the user asks for `/design-lint`, `$design-lint`, or wants the repo's DESIGN.md lint workflow. Read `.ai/commands/design-lint.md` and follow it as the source of truth.
---

# Design Lint

When invoked, read [../../commands/design-lint.md](../../commands/design-lint.md) and execute that workflow as written.

## Rules

- Treat the command file as canonical.
- In Codex, invoke this workflow with `$design-lint`.
- If the command file and this skill ever drift, update this skill to match the command.
